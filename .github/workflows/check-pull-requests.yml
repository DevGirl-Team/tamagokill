name: 📝 Tamagokill pull request check
on:
  pull_request:
    types: [opened, synchronize, edited]

jobs:
  conventionnal-commit:
    runs-on: ubuntu-latest
    steps:
      - name: 🔎 Check Conventional Commits
        id: check-for-cc
        uses: agenthunt/conventional-commit-checker-action@v1.0.0